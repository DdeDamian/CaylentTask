replicaCount: 2

image:
  repository: ddedamian/custom-nginx
  tag: v2.0.0
  pullPolicy: IfNotPresent

envvars:
  env: stage

nameOverride: "caylent-stage"
fullnameOverride: "caylent-stage"

service:
  type: ClusterIP
  # type: LoadBalancer # In case you want to deploy it on GCP and have easy external access
  port: 8080
  path: stage
