version: 2
jobs:
  build:
    docker:
      - image: ubuntu:18.04
        environment:
          TEST_VAR: test
    steps:
      - checkout
      - run:
          name: Update packages
          command: |
            apt update
            apt upgrade -y


version: 2
jobs:
  build:
    docker:
      - image: ubuntu:18.04
    steps:
      - checkout
      - run:
          name: Update packages
          command: |
            apt update
            apt upgrade -y
  # test:
  #   docker:
  #     - image: circleci/<language>:<version TAG>
  #   steps:
  #     - checkout
  #     - run: <command>
workflows:
  build_and_test:
    jobs:
      - build
      # - test
